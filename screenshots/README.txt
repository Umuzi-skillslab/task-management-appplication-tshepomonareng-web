Add your screenshots here before submitting:
- app-running.png        (the app open in a browser, with a few tasks added)
- tests-passing.png      (terminal output of `npm test` showing all tests passing)
- console-no-errors.png  (browser DevTools console, open, showing no errors)
- dom-features.png       (e.g. marking a task complete or deleting one)
